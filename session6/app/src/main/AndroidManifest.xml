<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Session6"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.RouteViewActivity"
            android:exported="false"
            android:label="RouteView Example"
            android:screenOrientation="sensor" />

        <activity
            android:name=".activities.DialViewActivity"
            android:exported="false"
            android:label="DialView Example"
            android:screenOrientation="sensor" />

        <activity
            android:name=".activities.ValueAnimatorActivity"
            android:exported="false"
            android:label="ValueAnimator Example"
            android:screenOrientation="sensor" />

    </application>

</manifest>
